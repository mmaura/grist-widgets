{
  "name": "notedeservice",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist node_modules/.vite",
    "build": "npm run clean && vite build --mode production",
    "dev": "vite",
    "serve": "npm run clean && vite build --mode dev && vite preview --mode dev",
    "deploy": "npm run clean && npm run build && ./deploy.sh"
  },
  "keywords": [],
  "author": "MAURA Mathieu",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "http-server": "^14.1.1",
    "prettier": "^3.7.2",
    "ssh2-sftp-client": "^12.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  }
}
